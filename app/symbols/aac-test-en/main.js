export default class LanguagePack {
    constructor() {
        this.name = "test-en";
        this.poss = [""];
        this.conjtype = [""];
        this.nounCondition = [""];
    }

    conjVerb(verb, tence, poss) {
        // Verb conjunction function
    }

    conjNoun(noun, type) {
        // Noun condition function
    }
    addPoss(verb, poss, tence, identifier) {
        // Possessor function
    }
}
